doctype html
head
  title=title
  link(href='/css/main.css', rel='stylesheet')
body
  h1 This is my Photo app
  div.container
    - for(var x = 0; x < images.length; x++)
      div.container__image
        img(src=images[x].url, alt="Cat Image")
